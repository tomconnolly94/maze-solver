#ifndef GRAPHENTRYPOINTS_H
#define GRAPHENTRYPOINTS_H

#include <GraphNode.h>

namespace Models
{
    struct GraphEntryPoints{
        GraphNode* startNode;
        GraphNode* endNode;
    };
}
#endif