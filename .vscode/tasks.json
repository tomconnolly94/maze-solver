{
    "tasks": [
        {
          "type": "shell",
          "label": "pre-build",
          "command": "./debug-build.sh",
          "options": {
            "cwd": "${workspaceFolder}"
          }
        }
      ],
      "version": "2.0.0"
}